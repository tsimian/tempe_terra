import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2c2c2b",
      dark: "#1e1e1e",
      light: "#565655",
    },
    secondary: {
      main: "#C1440E",
      dark: "#872f09",
      light: "#cd693e",
    },
  },
});

export default theme;
